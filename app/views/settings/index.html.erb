<%# FIXME: Needs nice displays of the form fields depending on the type. Main issue is parsing in the current value correctly. %>

<%= render partial: "settings/form", collection: @settings, as: :setting %>
